#include <bits/stdc++.h>
using namespace std;
int main()
{
    list<int> l = {4, 8, 65, 6, 5, 5, 9, 8, 23, 9, 4, 54, 17};
    //l.remove(10);
    //l.sort();
    //l.sort(greater<int>());
    //l.reverse();
    for (int val : l)
    {
        cout << val << endl;
    }

    return 0;
}